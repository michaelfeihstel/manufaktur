<% content_for :secondary_sidebar do %>
  <div id="secondary-sidebar">

    <div class="secondary-sidebar-header">
      <%= search_form_for @search do |f| %>
        <%= f.search_field :contact_name_cont, placeholder: "Suchen...", id: "secondary-search-field" %>
      <% end %>
    </div>
    
    <div class="section">
      <h4>Status</h4>
      <p>
        <label>Erstellt am</label>
        <%= @order.created_at.to_s(:short) %>
      </p>
      <p>
        <label>Liefertermin</label>
        <%= @order.date_delivery.to_s(:short) unless @order.date_delivery.nil? %>
      </p>
      <p>
        <label>Abgeschlossen am</label>
        <%= @order.try(:date_completed) %>
      </p>
    </div>


    <div class="section">
        <h4>Dokumente</h4>
        <ul class="side-nav">
          <li><%= link_to inline_icon("Auftragsbestätigung", "ion-document"), "#" %><li>
          <li><%= link_to inline_icon("Lieferschein", "ion-document"), "#" %><li>
          <li><%= link_to inline_icon("Rechnung", "ion-document"), "#" %><li>
        </ul>
    </div>    

  </div>
<% end %>