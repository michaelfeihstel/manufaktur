#chart_revenue

javascript:
  var de = d3.locale({
    'decimal': ',',
    'thousands': '.',
    'grouping': [3],
    'currency': ['€ ', ''],
    'dateTime': '%a %b %e %X %Y',
    'date': '%m/%d/%Y',
    'time': '%H:%M:%S',
    'periods': ['AM', 'PM'],
    'days': ['Sonntag', 'Montag', 'Dienstag', 'Mittwoch', 'Donnerstag', 'Freitag', 'Samstag'],
    'shortDays': ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'],
    'months': ['Januar', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'],
    'shortMonths': ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec']
  });


  var chart = c3.generate({
      bindto: "#chart_revenue",
      transition: {
        duration: 0,
      },
      data: {
        columns: [
          #{raw @line_items.map{|months, items| items.sum(&:price_total).to_i}.prepend("Umsatz")},
        ],
        type: 'area',
        labels: {
          format: {
            y: de.numberFormat('$'),
          }
        },
        colors: {
          "Umsatz": '#455A64',
          "Alle Aufträge (Vorjahr)": '#BFD2DB',
          "Shop": '#CAAE93'
        }
      },
      axis: {
        x: {
          tick: {
            centered: true
          },
          type: 'categories',
          categories: #{raw @line_items.map{|months, items| l months, format: "%b %Y"}}
        },
        y: {
          show: false,
          tick: {
            format: de.numberFormat("$")
          }
        }
      },
      tooltip: {
        format: {
          value: de.numberFormat('$')
        }
      },
      regions: [
        {start: 12.5, end: 13.5, class: 'highlighted-region'}
      ]
  });