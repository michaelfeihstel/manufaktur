li class="sidebar-item #{('active' if @order.id == order.id)}"
  a href= order_path(order)
    span.light= order.id
    span.right= completed_icon(order)
    span.right= marked_icon(order)
    br
    .truncated= order.billing_name