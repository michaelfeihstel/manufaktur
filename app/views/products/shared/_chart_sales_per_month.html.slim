.chart#chart_sales_per_month

javascript:
  var de = d3.locale({
    'decimal': ',',
    'thousands': '.',
    'grouping': [3],
    'currency': ['€ ', ''],
    'dateTime': '%a %b %e %X %Y',
    'date': '%m/%d/%Y',
    'time': '%H:%M:%S',
    'periods': ['AM', 'PM'],
    'days': ['Sonntag', 'Montag', 'Dienstag', 'Mittwoch', 'Donnerstag', 'Freitag', 'Samstag'],
    'shortDays': ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'],
    'months': ['Januar', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'],
    'shortMonths': ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec']
  });


  var chart = c3.generate({
    bindto: "#chart_sales_per_month",
    data: {
      columns: [
        #{raw @product.line_items.completed_since(13.months.ago).to_a.group_by_month(&:created_at).map{|k,v| v.map(&:quantity).sum.to_i }.prepend("Menge")}
      ],
      type: 'area',
      colors: {
        "Menge": '#451A64'
      }
    },
    axis: {
      x: {
        tick: {
          centered: true,
          outer: false
        },
        type: 'category',
        categories: #{raw @product.line_items.completed_since(13.months.ago).group_by_month("orders.completed_at", format: "%b %Y").count.map{|k, v| k}}
      },
      y: {
        show: false,
      }
    },
    tooltip: {
      format: { }
    }
  });