// set line_item.price input field
$('section[data-item-temp-id="' + <%= @line_item_temp_id %> + '"]').find('#price_single_input').val(
    <%= @price_single %>
);

// set #price_total span
$('section[data-item-temp-id="' + <%= @line_item_temp_id %> + '"]').find('#price_total').text(
    "<%= j (number_to_currency(@price_total, unit: "€ ")) %>"
);

// set topbar styling
$('section[data-item-temp-id="' + <%= @line_item_temp_id %> + '"]').find('.bar-top').attr({
    style: "<%= j ( product_color(@product) ) %>"
});