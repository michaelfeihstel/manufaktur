// set total quantity
$('section[data-line-item-id="<%= j @line_item_id %>"]').find("#quantity").text(
  <%= @quantity %>
);

// set span#price_total
$('section[data-line-item-id="<%= j @line_item_id %>"]').find("#price_total").text(
  "<%= number_to_currency(@price_total, unit: "€") %>"
);