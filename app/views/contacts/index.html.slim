= render "contacts/search"
= render "contacts/index/breadcrumbs"

- content_for :title_bar do
	.row
		.small-12.columns
			h1 Kontakte

= render "contacts/common/toolbar"
= render "contacts/index/sidebar"

- content_for :content do
	.row
		.small-12.columns
			= render "contacts/index/table_contacts"