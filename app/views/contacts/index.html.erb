<% @section = "Kontakte" %>

<!-- HEADER -->

<% content_for :breadcrumbs do %>
  <div class="row">
    <div class="small-12 columns">
      <ul class="breadcrumbs">
        <li><%= link_to "Home", "/" %></li>
        <li><%= render "/layouts/breadcrumbs/sections" %></li>
      </ul>
    </div>
  </div>
<% end %>

<% content_for :title_bar do %>
  <div class="row">
    <div class="large-9 small-9 columns">
      <h1 class="page-heading">Kontakte</h1>
    </div>
    <div class="large-3 small-12 columns">
      <ul class="inline-list right">
        <li>
          
        </li>
      </ul>
    </div>
  </div>
<% end %>

<!-- End Header -->

<!-- MAIN CONTENT -->

<% content_for :main_content do %>
  
    <!-- Quick Filter -->

    <%= render "contacts/search" %>

    <!-- End Quickfilter -->

    <!-- Contact Items
  
    <% @contacts.sort_by{|e| e[:name]}.each do |c| %>
      <div class="row">
        <div class="small-12 columns">
          <h3><%= link_to c.name, contact_path(c) %></h3>
          <hr>    
        </div>
      </div>
    <% end %>

    End Contact Items -->

    
    <% @contacts.sort_by{|e| e[:name]}.each do |c| %>
      <div class="row">
        <div class="small-12 columns">
          <h4>
            <%= link_to c.name, contact_path(c) %>
            <small><%= c.addresses.first.full_address(1)%></small>
            <% if c.addresses.count - 1 > 0 %>
              <span class="label"> + <%= c.addresses.count - 1 %></span>
            <% end %>
          </h4>
          <ul class="inline-list">
            <li><%= link_to "Aufträge", "#" %></li>
            <li><%= link_to "Briefe", "#" %></li>
            <li><%= link_to "Bemerkungen", "#" %></li>
          </ul>
          <hr>
        </div>
      </div>
    <% end %>
      
    


<% end %>