#chart_product_family_share

javascript:
  var chart = c3.generate({
    bindto: "#chart_product_family_share",
    data: {
      columns: [
        #{ raw collection.map{ |item| item.total }.prepend("Absatz") }
      ],
      type: 'bar',
    },
    axis: {
      rotated: true,
      x: {
        type: 'categories',
        categories: #{raw collection.map{ |item| item.name } },
      },
    }
  });