= nested_form_for @retailer do |f|

  - if @retailer.errors.any?
    #error_explanation
      h4 = "#{pluralize(@retailer.errors.count, 'error')} haben das Speichern dieses Kontakts verhindert:"
      ul
      - @retailer.errors.full_messages.each do |msg|
        .alert-box.alert data-alert=""
          li = msg

  ul.tabs data-tab=""
    li.tab-title.active = link_to "Stammdaten", "#tab_general"
    li.tab-title = link_to "Bemerkungen", "#tab_notes"
    li.tab-title = link_to "Briefe", "#tab_letters"
  .tabs-content
    = render "retailers/edit/tab_general", f: f
    = render "retailers/edit/tab_notes", f: f
    = render "retailers/edit/tab_letters", f: f

    = link_to 'Abbrechen', :back, class: "button secondary left"
    = f.submit "Speichern", class: "button right"