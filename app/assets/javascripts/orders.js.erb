$('#order_contact_id').change(function() {

	var order_id = $('form').data('item-id');

	console.log('current order_id: ' + order_id);

  $('#select_addresses').load (
  		"/orders/" + order_id + "/update_addresses",
  		{ "contact_id": $('#order_contact_id').val() }
  );

});